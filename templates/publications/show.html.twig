{% extends 'base.html.twig' %}

{% block title %}Publications{% endblock %}

{% block body %}
    <h1>Publications</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publication.id }}</td>
            </tr>
            <tr>
                <th>Images</th>
                <td>{{ publication.images ? publication.images|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ publication.content ? publication.content|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ publication.createdAt ? publication.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_publications_index') }}">back to list</a>

    <a href="{{ path('app_publications_edit', {'id': publication.id}) }}">edit</a>

    {{ include('publications/_delete_form.html.twig') }}
{% endblock %}
