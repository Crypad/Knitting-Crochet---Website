<form method="post" action="{{ path('app_publications_sharing_delete', {'id': publications_sharing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publications_sharing.id) }}">
    <button class="btn">Delete</button>
</form>
